import UIKit

let myfrinednaem : String = "Ahmad"
let temperature : Int = 42


if (temperature == 44 || temperature > 44 ) {
    print (" \(myfrinednaem) it is hot . Don't go outside.")
}

else if (temperature < 7 ) {
    print (" \(myfrinednaem) it is freezing . Don't go outside")
}
else {
    print (" \(myfrinednaem) it is ok . You can go outside.")
}
